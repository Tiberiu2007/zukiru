# event — Layer 1 (services). Type-safe publish/subscribe event bus with both
# synchronous and deferred (queued) dispatch. Header-only. Depends on core.
# Namespace zuki::event.
add_zuki_module(event PUBLIC_DEPS core)
